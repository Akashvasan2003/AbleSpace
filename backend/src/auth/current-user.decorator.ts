// Re-export from canonical location to avoid breaking any existing imports.
export { CurrentUser } from '../common/decorators/current-user.decorator';
export type { AuthUser } from '../common/decorators/current-user.decorator';
